#!/usr/bin/env npx tsx

const ERR =
  "parser: not implemented (stage: scaffolding). " +
  "See roadmap/parser-refactor/INDEX.md for the current state of the refactor.";

process.stderr.write(ERR + "\n");
process.exit(2);
